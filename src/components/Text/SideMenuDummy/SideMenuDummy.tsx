import theme from '../../Theme/theme';
import styled from 'styled-components';

const SideMenuDummy = styled.a`
	color: ${theme.color.background};
	text-decoration: none;
	font-size: 3em;
	font-weight: 700;
`;

export default SideMenuDummy;
